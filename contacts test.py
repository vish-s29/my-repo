# count the numbers of times the values get entered in list (it shld count only those which gets repeated more than once)
def occurence():
    list = [1,2,3,3]
    for x in list:
        if x == list:
           print ("the numbers that gets rep",x)
occurence()

    